# AWS region
aws_region: eu-west-1

# AWS Key Pair name and path
aws_keypair: ansible-kp
ansible_ssh_private_key_file: ./ansible-kp.pem

# RDS credentials
mysql_user: admin
mysql_password: T@sk1_An$ible